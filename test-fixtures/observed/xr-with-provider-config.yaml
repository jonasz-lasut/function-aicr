apiVersion: example.org/v1
kind: GPUStack
metadata:
  name: cs-resolve
  namespace: default
spec:
  gpuType: h100
  cloud: kind
  purpose: training
  providerConfigKind: ProviderConfig
  providerConfigName: workload-a
